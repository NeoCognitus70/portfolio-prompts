interface:
  display_name: "Onboard Project"
  short_description: "Onboard an existing repository through staged PRs"
  default_prompt: "Use $portfolio-prompts:onboard-project to onboard <project-folder>."
policy:
  allow_implicit_invocation: false
