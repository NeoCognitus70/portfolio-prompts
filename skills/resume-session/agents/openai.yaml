interface:
  display_name: "Resume Session"
  short_description: "Resume a project from its handover and backlog"
  default_prompt: "Use $portfolio-prompts:resume-session to resume <project-folder>."
policy:
  allow_implicit_invocation: true
