interface:
  display_name: "Run Worklist Item"
  short_description: "Execute one validated item from a project worklist"
  default_prompt: "Use $portfolio-prompts:loop-worklist to execute the next item for <project-folder>."
policy:
  allow_implicit_invocation: false
